"""Discrete neural flow samplers for constrained lattice targets.

Subpackages: `targets` (Ising, Potts, cluster expansion), `models` (leTF
backbones), `constraints` (swap heads and composition channels), `samplers`
(training and Euler integration), `mcmc` (reference chains), `diagnostics`
(ESS, FLOPs, observables). See src/README.md for the module map.
"""
